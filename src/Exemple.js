import React from 'react';


const myFunction=() =>{
   
    if (confirm("Press a button!")) {
         onClick ={() => props.deleteUser(user.id)} 
        
    } else {
         onClick ={() => props.deleteUser(user.id)} >Non</button>
    }
    
  }
  export default myFunction